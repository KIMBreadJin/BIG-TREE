<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.mapper.ReportMapper">
	<insert id="regist">
		insert into big_tree_report(bno,reason,details,reporter)
		 values (#{bno},#{reason},#{details},#{reporter})
	</insert>
	<delete id="delete">
		delete big_tree_report where bno=#{bno} and reporter= #{reporter}
	</delete>
	<select id="getReportList" resultType="com.green.vo.ReportVO">
		select*from big_tree_report where bno=#{bno}
	</select>
	<select id="getReportCount" resultType="int">
		select count(reporter) from big_tree_report where bno=#{bno}
	</select>
	<select id="getReport" resultType="com.green.vo.ReportVO">
		select*from big_tree_report where bno=#{bno} and reporter=#{reporter}
	</select>
	<update id="update">
		update big_tree_report set reason=#{reason}, details=#{details},
		 	updateDate = sysdate where bno = #{bno} and reporter=#{reporter}
	</update>
</mapper>